function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // This Promise doesn't do anything specific yet, 
    // it's just a simple Promise that resolves immediately
    resolve();
  });
}

export default getResponseFromAPI;
