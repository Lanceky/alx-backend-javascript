function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // This Promise doesn't do anything specific
    // It's just an example of returning a Promise
  });
}

export default getResponseFromAPI;
